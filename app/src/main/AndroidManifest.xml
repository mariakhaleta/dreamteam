<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mariakhaleta.booksstore">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Regestration">
        </activity>

        <activity android:name=".MainContentLayout"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".BooksListView"
            android:theme="@style/AppTheme"/>
        <activity android:name=".BookInfoActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".UserOwnCabinet"
            android:theme="@style/AppTheme"/>
        <activity android:name=".StoreListView"
            android:theme="@style/AppTheme"/>
        <activity android:name=".WishListView"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>